//February 4, 2022
String Functions
  .infexOf("d") - returns first time we see "d"
  .substring(x)
  .substring(x,y)
  .compareTo() - compare the ASCII values of strings: how we order and sort strings
  .equalsIgnoreCase() 

if(boolean)? true : false;

word.substring(word.indexOf(" ") + 1) //returns a word (in a string?)
//starts at x, ending at y - 1

2 letter word --> -1 when looping through strings
substring(i, i + 2) for a 2 letter word

int(Math.random)) --> TRUNCATES (chops off decimals as math.random generates no numbers above .999)
